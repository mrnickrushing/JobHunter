[build]
buildCommand = "cd backend && npm install --omit=dev && cd .. && npm run build"

[deploy]
startCommand = "node backend/server.js"
healthcheckPath = "/api/health"
healthcheckTimeout = 300

[[volumes]]
mountPath = "/app/data"
